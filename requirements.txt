# Core modelling and evaluation — everything the pipeline actually needs.
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.4
lightgbm>=4.0
joblib>=1.3

# Figures
matplotlib>=3.8

# Serving
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# Dashboard
streamlit>=1.30
plotly>=5.18

# Tests
pytest>=8.0

# Optional. Each is detected at runtime and skipped with a printed note when
# absent, so a failed install here never blocks training or serving.
#   econml     — CausalForestDML, joins the model comparison when present.
#                Installed for the reported results; uncomment to reproduce
#                the causal-forest row.
#   mlflow     — experiment tracking for the learner comparison
#   evidently  — feature-drift HTML report alongside the built-in uplift monitor
# econml>=0.15
# mlflow>=2.10
# evidently>=0.4
